/*
    Let's set 2 variables, p1 and p2.
    Initialize with -1
    Iterate through the array, if word1 is found, set p1 to the index and if word is found, update p2 to index

    If both are not -1, then exit the loop

    return the diference.

    T - O(n)
    S - O(1)
*/